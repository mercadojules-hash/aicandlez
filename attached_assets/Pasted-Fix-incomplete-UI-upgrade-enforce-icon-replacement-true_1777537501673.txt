Fix incomplete UI upgrade — enforce icon replacement, true glass/bevel effect, and real logo usage.

Current state is NOT correct:

* Timeline icons (Today’s Plan) are still old/basic
* Cards lack visible glass/bevel depth
* Natura AI logo is NOT being used (text only)

These must be FIXED with real implementation changes.

---

1. USE ACTUAL NATURA AI LOGO IMAGE (NOT TEXT)

---

REQUIREMENT:

* Import and use the REAL logo image asset (PNG/SVG)
* Do NOT simulate logo with text

HEADER LAYOUT:

[ LOGO IMAGE ]  Natura AI
AI Wellness Coach

Implementation:

* Logo image height: 28px
* Positioned LEFT of text
* Horizontal flex container

Text styling:

"NATURA AI"

* color: #FFFFFF
* font-weight: 600

"AI WELLNESS COACH"

* color: #9FE870
* font-size: smaller
* letter-spacing: slight

Spacing:

* 8px between logo and text
* 2–4px between title and subtitle

If logo asset is missing:

* locate it in project assets OR request path
* DO NOT skip this step

---

2. FORCE REPLACEMENT OF TODAY’S PLAN ICONS

---

IMPORTANT: Current icons were NOT replaced.

Replace ALL icons in timeline rows with NEW components.

Each row MUST use:

* Remedy → Leaf icon
* Recipe → Bowl/Food icon
* Action → Lightning icon
* Wellness → Lotus icon

DO NOT reuse old icons.

---

3. IMPLEMENT ICON COMPONENT (REQUIRED)

---

Create reusable component:

function PremiumIcon({ icon }) {
return (
<div style={{
width: 44,
height: 44,
borderRadius: '50%',
background: 'rgba(255,255,255,0.05)',
display: 'flex',
alignItems: 'center',
justifyContent: 'center',
boxShadow: '0 0 18px rgba(120,255,180,0.18)',
border: '1px solid rgba(255,255,255,0.08)'
}}>
{icon} </div>
);
}

Use this component in EVERY timeline row.

---

4. ADD CHEVRON ARROWS (ENFORCE)

---

Each row must include:

ChevronRight icon

Position:

* Far right
* Vertically centered

If missing, explicitly add it.

---

5. TRUE GLASS / BEVEL EFFECT (STRONGER — NOT CURRENT VERSION)

---

Current cards are too flat. Increase depth.

UPDATE ALL CARDS TO:

style={{
background: 'linear-gradient(145deg, rgba(255,255,255,0.08), rgba(0,0,0,0.25))',
backdropFilter: 'blur(16px)',
borderRadius: 20,
border: '1px solid rgba(255,255,255,0.12)',
boxShadow: `     inset 0 1px 1px rgba(255,255,255,0.15),
    inset 0 -1px 2px rgba(0,0,0,0.4),
    0 12px 30px rgba(0,0,0,0.35)
  `
}}

This creates:

* top highlight (bevel)
* inner shadow (depth)
* outer shadow (floating card)

---

6. APPLY SAME EFFECT TO:

---

* Stats bar
* Timeline cards
* “You’re doing great” card
* “Need guidance” card

NO exceptions.

---

7. FIX STAT ICONS (TOP ROW)

---

Wrap ALL stat icons inside PremiumIcon component.

Remove any emoji usage.

---

8. FINAL VALIDATION (DO NOT SKIP)

---

Before finishing:

CHECK:

* Logo image visible top left ✔
* Timeline icons replaced ✔
* Icons inside glowing circles ✔
* Chevron arrows present ✔
* Cards visibly beveled (not flat) ✔

If any item fails, continue fixing.

---

## EXPECTED RESULT:

* Premium glass UI
* Clear glowing icons
* Real branding (logo)
* Consistent depth across all cards

Apply changes and reload preview.
