STOP. Do not create another webhook. Do not rotate secrets. Do not investigate Kraken. Do not generate another large report.

Assume the following are already confirmed:

* New Stripe webhook created
* New webhook signing secret copied from Stripe
* STRIPE_WEBHOOK_SECRET updated in Render
* Render redeployed successfully
* STRIPE_WEBHOOK_DIAG logs are present in production
* STRIPE_WEBHOOK_DIAG2 logs are present in production
* webhookSecretLoaded=true
* webhookSecretPresent=true
* StripeSignatureVerificationError still occurs

At this point, stop investigating secret replacement and focus only on determining WHY signature verification is failing.

Please answer these four questions with evidence from the actual production code:

1. RAW BODY VERIFICATION

* Show the exact middleware order in app.ts.
* Is express.json() executed before the Stripe webhook route?
* Does /api/stripe/webhook use express.raw({ type: 'application/json' })?
* Immediately before stripe.webhooks.constructEvent(), log:

  * typeof req.body
  * Buffer.isBuffer(req.body)
  * req.body.constructor?.name

Expected result:

* Buffer.isBuffer(req.body) MUST be true.

2. SECRET SOURCE VERIFICATION

* Identify the exact variable passed into:
  stripe.webhooks.constructEvent(...)
* Confirm it is the SAME variable being logged by STRIPE_WEBHOOK_DIAG.
* Verify there is not a second secret source such as:

  * STRIPE_SIGNING_SECRET
  * config.stripe.webhookSecret
  * secrets.webhookSecret
  * cached configuration
  * legacy environment variables

3. STRIPE EVENT CORRELATION

* Capture one failing Stripe event ID from Stripe Dashboard.
* Search production logs for that exact event.
* Confirm the failing event is reaching:
  /api/stripe/webhook
* Confirm the Stripe-Signature header exists on the request.

4. LIVE VS TEST MODE VALIDATION

* Verify the webhook endpoint is Live mode.
* Verify the failing event is Live mode.
* Verify the signing secret being loaded belongs to that exact endpoint.

Return only:

* middleware order
* Buffer.isBuffer(req.body) result
* exact secret variable source
* matching event ID verification
* live/test verification

No code changes yet.
No new webhooks.
No secret rotation.
No Kraken investigation.
No trading logic investigation.

The goal is to identify the root cause of StripeSignatureVerificationError, not to restart troubleshooting.
