- [Stripe webhook secret resolution](stripe-webhook-secret-resolution.md) — env STRIPE_WEBHOOK_SECRET first, silent DB fallback (stripe._managed_webhooks) shadows a missing/empty Render env var with a stale dev secret → "No signatures found"; two constructEvent paths, only stripe-replit-sync throws.
- [AI-trading activation banner reconciliation](activation-banner-reconciliation.md) — false "ACTIVATION FAILED" banner = sticky local enable-error never reconciled vs polled `enabled`; clear + render-gate, scope by action enable/disable so genuine disable failures still show.
- [Customer equity sourcing](customer-equity-sourcing.md) — LIVE headline equity = ACTIVE exchange only (`activeEquityUSD`), never the sum; `totalEquityUSD` is for the Connected Exchanges panel only.
- [Coinbase portfolio scope](coinbase-portfolio-scope.md) — "account is not available"/INSUFFICIENT_FUND while equity reads fine = key's tradable portfolio ≠ funded portfolio (we omit retail_portfolio_id); risk equity = per-user isDefault live conn, prices ALL holdings so overstates deployable USD.
- [sim-registry field hydration](sim-registry-field-hydration.md) — new sim_positions cols copied to sim_trades at close MUST also be mapped in loadFromDB() positions map, else Render restarts strand them null. Typecheck won't catch (optional field).
- [Coinbase live-order compliance](coinbase-symbol-mapping.md) — adapter.normaliseSymbol is the authoritative converter at submit; validate against product spec (precision/min-size/min-notional/tradability) locally before sending or the broker rejects one trade at a time.
- [Customer ARM visual gate](customer-arm-visual-gate.md) — green "AI EXECUTION ARMED" must follow per-session armedForLive (resets false on load), not persisted autoMode; fail-closed when runtime unresolved, else UI lies while backend returns LIVE_BLOCKED_NOT_ARMED.
- [ARM enforcement model](arm-enforcement-model.md) — armedForLive trusted via request body (interlock, not forgery-proof); manual /api/user/live-order NOW server-checks it (412 runtime_not_armed); AI auto per-tick path does NOT re-check ARM (only at activation). Real-money safety = kill switch + liveReady + risk gates.
- [How prod deploys (Render autoDeploy)](render-deploy-mechanism.md) — prod=Render, autoDeploy on origin/main push; agent can't force a build; suggestDeploy/getDeploymentInfo point at a DIFFERENT Replit target, not aicandlez.com; verify via prod DB + public engine/status.
- [Prod DB lives on Render, not Replit](prod-db-on-render.md) — executeSql prod hits an EMPTY Replit replica; use RENDER_PROD_DATABASE_URL via pg. SCHEMA DRIFT TRAP: dev `drizzle-kit push` never reaches prod; a schema col missing in prod breaks BOTH reads+writes (Drizzle explicit col lists) → whole table data layer down.
- ["TRADE EXECUTED" toast is decoupled](trade-executed-toast-decoupled.md) — portal toast/session-counter fire off global engine signal telemetry (uid=null), NOT real Coinbase fills; authoritative live-fill = execution_submitted_coinbase tagged with the user's userId + a sim_positions/sim_trades row.
- [Live position exit governance](live-position-exit-governance.md) — per-user LIVE exits via runHardStopMonitor: SL→TP→LIVE trailing→LIVE max-hold (EXIT_DEFAULTS.maxHoldHours, now 6h universal, price-independent); SL/TP synthetic+locked at open; global trailing engine is paper-only; no AI exit.
- [Customer runtime exchange selection](runtime-exchange-selection.md) — picked exchange reaches the engine only via the GET /runtime-state cohort writeback (isDefault/live); canTrade must be enforced at ALL gates (switcher, PUT, healthyLive, AND open-path execution), or stale rows route real money to an unauthorized venue.
- [QA account entitlement exemption](qa-account-exemption.md) — `users.is_internal_account` exempts QA accts from tier daily/concurrent caps; liquidity+risk+sizing+balance STILL enforced; predicates fail-closed.
- [Execution-funnel telemetry](execution-funnel-telemetry.md) — diagnose 0-trade via single executionStreamBus subscriber (zero churn); all 5 gates are PRE-attempt; operator(per-signal)/customer(per-user) counting grains differ.
- [Signal funnel telemetry](signal-funnel-telemetry.md) — two funnel modules (executionFunnel block-count vs signalFunnel true per-signal); trust signalFunnel for "where signals die"; count executionAttempted exactly once or exchange-rejects double-count. Funnel collapses at confidence/MTF gate.
- [Confidence pipeline](confidence-pipeline.md) — execution vs display confidence are separate; `executionEligible` is the ONE execution authority; never add a 2nd confidence gate or a global daily throttle (per-user entitlement owns daily).
- [Trade activity drop](trade-activity-drop.md) — fewer trades now = testMode default flip (true→false ~May 26, master bypass) + layered execution gates added after May 15, NOT confidence-formula drift. Check testMode/gates/paper-default before re-investigating the scorer.
- [Prod live-exec verification](prod-live-exec-verification.md) — execution_submitted/order_accepted/order_rejected ARE in DB logs (with userId); CUSTOMER_FANOUT_*/POSITION_* are pino-only (Render, not DB). Reconcile accepts vs sim_positions.created_at / sim_trades.entry_time.
- [Broker-accept-without-position gap](broker-accept-without-position-gap.md) — a 2nd concurrent same-user+symbol broker fill is accepted upstream but dropped at the per-symbol mirror-write guard → real-money untracked fill; submit precedes position write, non-transactional.
- [Customer attribution chokepoint](customer-attribution-chokepoint.md) — per-customer funnel recorded at executeCustomerOrder; MUST record on BOTH normal return AND throw path or attempts undercount.
- [Realized P&L ledger & pollution](realized-pnl-ledger-pollution.md) — operator/customer realized = stored `sim_accounts.total_realized` (net of app fees), not live-recomputed; incident pollution = sim_trades exchange NULL + null order IDs + close_reason RECONCILED_BACKLOG; clean realized = exchange IS NOT NULL only.
- [Portal shell role dispatch](portal-shell-role-dispatch.md) — "customer portal reverted to admin shell" is usually a role promotion (operator allowlist re-asserts admin on every /auth/me), NOT a build rollback; diagnose role before deployed SHA.
- [Exit engine two-book invariant](exit-engine-two-books.md) — global `trades` book (= maxActivePositions cap source) and per-user `sim_positions` are SEPARATE with separate exit monitors; every exit trigger must close the GLOBAL book (closePosition+markTradeRowClosed) or the cap saturates and AI execution silently stops at Gate 1.
- [Multi-exchange parallel cohort](multi-exchange-parallel-cohort.md) — per-user parallel live trading: runtime writeback MUST demote stale non-healthy live venues to paper (not just promote), or the engine cohort keeps fanning out a degraded venue; caps/dedup scoped per (user,exchange).
- [Gate 1 starves customer fan-out](autoexecute-gate1-starves-fanout.md) — autoExecute Gate 1 (global trades book cap) returns early ABOVE the customer live + paper fan-out, so a saturated global book = 0 customer exec attempts even with free per-user capacity; funnel signature reachedExecution>0 & passedPositionLimits==0.
- [Customer LIVE decoupled from global mode](customer-live-decoupled-from-global.md) — customer live AI fan-out gated only by CUSTOMER_LIVE_EXECUTION_ENABLED+per-customer gates, runs every autoExecute pass (sim+live); live cohort excluded from paper fan-out (different tradingMode cols); global-sim failure never strands independent sim_positions fills.
- [Live-money env knobs must fail-safe](live-money-env-knobs-failsafe.md) — bare Number(env) on a real-money exit/stop knob → NaN silently fail-OPENs the stop; always validate finite+range and fall back to a safe default.
- [Live SL blow-through = monitor latency](live-sl-blowthrough-latency.md) — LIVE stops closing at -4%/-5.4% on a 2% stop was 60s-tick + multi-tick-confirm drift, NOT stop math; fix = fast cadence + immediate-fire tier inside catastrophic band, guarded against overlap. Check cadence before scorer.
- [Customer funnel window trap](customer-funnel-window-trap.md) — getCustomerFunnel counters are in-memory since-process-start (reset on restart); never blend with all-time persisted sim_trades stats unscoped — carry `since` through and label conversions "since engine start".
- [aicandlez-app transport trap](aicandlez-app-transport-trap.md) — PWA has both api.ts (cookie-only, no Bearer) AND authFetch; new /api calls MUST use authFetch per locked invariant; check-no-bare-api-fetch does NOT catch api.* so green checks ≠ compliance.
- [Live max-hold close = broker-dependent (zombies)](live-maxhold-broker-close-zombies.md) — max-hold (EXIT_DEFAULTS, now 6h universal) DOES fire; stuck rows = broker close rejected every tick (dust/min-notional/balance); balance-verified reconciler retires true orphans locally; reject reason in user_notifications.
- [Exit-config counterfactual & pnl semantics](exit-config-counterfactual.md) — SL/TP/trailing act on RAW price (TP=+4% raw, SL≈−2%); realized_pnl_pct is fee+rounding-scaled (eff notional << size_usd). MFE shows live profitability issue is NOT TP-too-low/trail-too-tight; raising TP4→5 loses the 4% cluster, widening trail too wide for sub-3% peaks.
- [SELL-only 1H trend filter](sell-only-1h-trend-filter.md) — engine's require1HTrend is a BOTH-SIDES alignment gate (blocks counter-trend BUY too); a SELL-only short filter needs a SEPARATE live-exec gate, never reuse require1HTrend.
- [Throughput/concurrency analysis trap + live max-hold gap](throughput-backlog-maxhold.md) — separate closed-hold-time (shorter on all-SL stretch) from open-concurrency integral (can balloon from a pre-activation backlog, NOT the new config); flag: live positions seen open 40–56h past 24h max-hold.
- [Per-exchange size override ignored = deploy gap](coinbase-perexchange-override-deploygap.md) — DB override + correct source + git-ancestry but every fill uses global size = running build predates the per-exchange code; never use max-hold behavior as proof of running build (env LIVE_POSITION_MAX_HOLD_MS resolves 1h on any build).
- [Kraken nonce + operator single-owner](kraken-nonce-single-owner.md) — EAPI:Invalid nonce = same-process Date.now() ms collisions + cross-process interleave on shared env key; fix = process-global monotonic nonce + one owner (prod && !REPL_ID); customer vault-key path NOT gated; scale-out needs OPERATOR_ENGINE_OWNER=false on all but one.
