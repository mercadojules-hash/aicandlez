// Desktop Edition stub for `@clerk/themes` (aliased in vite.config.ts).
// The Clerk stub ignores appearance, so an empty theme object is sufficient.
export const dark: Record<string, unknown> = {};
export default { dark };
